import { install, Store } from './store'
import { mapState, mapMutations, mapActions, mapGetters } from './helper'
export default {
    install,
    Store,
    mapState,
    mapMutations,
    mapActions,
    mapGetters
}

export {
    install,
    Store,
    mapState,
    mapMutations,
    mapActions,
    mapGetters
}